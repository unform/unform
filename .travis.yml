language: node_js

node_js:
  - "10"

cache:
  directories:
  - node_modules

install:
- yarn

test:
- yarn coveralls

deploy:
  provider: npm
  email: "$EMAIL"
  api_key:
    secure: "$API_KEY"
  on:
    tags: true
    repo: rocketseat/unform
